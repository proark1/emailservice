node_modules/
dist/
web/dist/
.env
certs/
*.pem
*.log
.DS_Store
coverage/
# Written by the openapi-breaking-changes CI job (must live in $GITHUB_WORKSPACE
# so the dockerized oasdiff action can read it).
openapi-base.json
